{
  "$schema": "../../../schemas/target-config.schema.json",
  "name": "opencode-launcher",
  "displayName": "OpenCode optimized launcher",
  "configDir": "~/.local/bin",
  "configFile": "opencode",
  "instructionsFile": "opencode",
  "auxiliary": true,
  "distribute": {
    "opencode": { "source": "packages/runtimes/opencode-launcher/opencode" }
  },
  "executables": ["opencode"],
  "notes": [
    "~/.local/bin must precede the real OpenCode install directory in PATH",
    "Sets OPENCODE_DISABLE_CLAUDE_CODE_SKILLS before process startup, then execs the real binary"
  ]
}
