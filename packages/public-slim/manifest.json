{
	"name": "harunon-harness-slim",
	"description": "Public slim distribution: cross-runtime AI coding harness (SSOT + declarative distribution + safety hooks), with personal tooling documents and org-specific config removed.",
	"notes": [
		"このリポジトリが SSOT。public 側は scripts/build-public-slim.py の生成物で、public を直接編集しない（編集すると次の生成で無言で巻き戻る）。ロジックは scripts/harness_lib/public_slim.py",
		"include は allowlist。公開は不可逆なので、新規ファイルがデフォルトで漏れる denylist 方式は採らない",
		"個人好みのツール（rtk / mise / gwm）の『文書』は slim から外すが、『hook 実体』は残す。rtk-rewrite.sh は rtk 未導入なら exit 0 で素通り、block-grep-in-bash.sh は rtk を参照しない。hook は policy/hook-pipeline.json・settings.json・全 target config・テストに配線されているので、外すと参照が全部 dangling になる（初版 manifest で外して気づいた）",
		"生成物は 2 つ。harunon-harness-slim（repo レイアウトの allowlist コピー）と harunon-pi-agent-slim（harness-slim から distribute.py pi で組んだ ~/.pi/agent payload + packages/public-slim/pi-agent/ の install / validate スクリプト）。pi payload を private tree ではなく slim から導くので、slim で落としたものが pi 側に混ざらない"
	],
	"exclude": [
		"packages/extras/",
		"packages/restricted/",
		".gitmodules",
		"plans/",
		".rulesync/",
		"rulesync.jsonc",
		"rulesync.lock",
		"packages/core/RTK.md",
		"packages/core/fragments/claude-md/",
		"packages/core/lessons/lessons.json"
	],
	"excludeReasons": {
		"packages/extras/": "会社・契約固有（private submodule）。参照ごと落とす。target config の source リストに残る packages/extras/_active/... は dropExcludedSources が落とす",
		"plans/": "個人の作業記録。絶対パスの漏れ元（plans/015-*.report.md に /Users/<name>/ が入っている）",
		"packages/core/RTK.md": "rtk 前提の常駐文書。rtk 未導入の読者には毎セッション無効な指示を読ませるだけ。各 target config の RTK.md 配布エントリは dropExcludedSources が落とす",
		"packages/core/fragments/claude-md/": "個人ツール（rtk / gwm）前提の CLAUDE.md 節。配布時は expandIncludes で展開されるので自分の環境の内容は変わらない。CLAUDE.md 側の include 行は dropIncludes が落とす",
		"packages/core/lessons/lessons.json": "個人の作業履歴。公開価値の判断が別途必要なので初版では出さない",
		"packages/restricted/": "ライセンス上 public 再配布不可の vendored 区画（ADR-002 Update 2026-09-11）。include に入れないので allowlist 上は自動的に落ちる。ここに列挙するのは意図の明示のため。他の vendored skill は improve=MIT / skill-creator=Apache-2.0 / source-driven-development=MIT で再配布可",
		".github/workflows/ci.yml": "include に .github/ を入れていないので落ちる。private の CI は rulesync / mise.global.example.toml / extras 前提の step を持ち slim では回らないため、addFiles で slim 用 ci.yml を置く"
	},
	"include": [
		"packages/core/",
		"packages/targets/",
		"packages/runtimes/",
		"packages/public-slim/",
		"scripts/",
		"schemas/",
		"docs/adr/",
		"docs/diagrams/",
		".githooks/",
		"CONTEXT.md",
		"CLAUDE.md",
		".claude/skills/",
		"requirements.txt",
		".gitignore",
		".env.example",
		"mise.toml",
		"mise.global.example.toml"
	],
	"includeNote": "CLAUDE.md（repo のプロジェクト指示）と .claude/skills/（sync-settings / add-skill-to-repo。この repo を触る作業者向けの skill）は開発 workflow の一部なので出す。scripts/tests の documentation contract がこれらを読む。.claude/ 配下で追跡されているのはこの 2 skill だけ（settings.local.json 等は gitignore）",
	"keptTooling": {
		"mise.toml": "toolchain の pin（node / python / rulesync）。個人情報を含まず、bootstrap.sh・pre-push・テスト（_helpers.make_bootstrap_repo / check-tool-updates / pinned-tool-version）が実体を前提にする。外すと slim 側で 20 クラス近いテストが落ちるので残す（初版 manifest では外していた）。mise.global.example.toml も同じ理由で残す",
		"packages/core/hooks/rtk-rewrite.sh": "rtk 未導入なら exit 0（advisory rewrite hook）。残しても害がなく、外すと hook-pipeline.json / settings.json / target config / テストが dangling になる",
		"packages/core/hooks/enforce-gwm-for-worktree.sh": "gwm 未導入でも fail-closed（rigor profile が casual なら素通り）。README で gwm が要ることを明記する。外すには hook-pipeline.json / danger-rules.json / テストの再構成が要り、slim の初版では見送る"
	},
	"dropIncludes": true,
	"dropIncludesNote": "exclude された fragment を指す `<!-- include: ... -->` 行は、生成時に行ごと削除する。fragment が無いまま残すと dangling になり（expand_includes は FileNotFoundError で fail fast）、slim 側で配布が回らない。判定は『include 先が出力 tree に無い』の完全一致なので exclude の宣言から機械的に導ける",
	"dropExcludedSources": true,
	"dropExcludedSourcesNote": "出力 tree の packages/targets/*/config.json について、distribute エントリの source が出力に無ければエントリごと落とす（文字列 source）。リスト source は無い要素だけ落とし、空になればエントリを落とす。settingsSync.source が無ければ settingsSync を落とす。これで RTK.md の配布エントリと extras の rules が slim の distribute から消え、slim 側で distribute.py <target> --push が回る",
	"dropGhostCommands": true,
	"dropGhostCommandsNote": "commands.md の表から、出力 tree に skill 実体が無いコマンド（restricted / extras / rulesync curated 由来）の行を落とす。known の判定は validator（commands-vs-skills）と同じ関数。表以外の本文に ghost が残れば build を落とす（SSOT 側で直す）",
	"rewriteScaleCounts": true,
	"rewriteScaleCountsNote": "CONTEXT.md「管理規模」の数字を出力 tree の実体で書き直す。restricted の skill が落ちて skills の数が変わるため。数え方は validator（context-scale-counts）の scale_actuals を共有する",
	"addFiles": {
		"LICENSE": "packages/public-slim/LICENSE",
		"README.md": "packages/public-slim/README.harness-slim.md",
		".github/workflows/ci.yml": "packages/public-slim/ci.harness-slim.yml",
		"packages/core/lessons/lessons.json": "packages/public-slim/lessons.empty.json"
	},
	"addFilesNote": "dest（出力 tree の相対パス）→ source（この repo の相対パス）。README は slim 用に書き起こす（何が入っていて何が入っていないか、どのランタイムに配布できるか、生成物であって SSOT でないこと）。lessons.json は個人履歴を落とす代わりに空の ledger を置く（validator の lessons check がファイル不在を error にするため）。core-standards.md の一般化は 7a670df で SSOT 側に反映済みなので rewrite は無い",
	"piAgent": {
		"staticDir": "packages/public-slim/pi-agent/",
		"installManifest": "install-manifest.json",
		"note": "harness-slim の packages/targets/pi/config.json（dropExcludedSources 適用後）から distribute の dest キーと settingsSync.keys を install-manifest.json に書き出す。install.sh はそれを jq で読んで rsync / settings merge する（管理パス配列を shell に直書きしない）。distribute --push が dest に残す ledger / backups は payload から外す（生成マシンの repo パスが載るため）"
	},
	"gate": {
		"_comment": "生成後のゲート。1 件でも当たれば build を落とす（exit 1）。両方の生成物を走査する。公開は不可逆なので CI でも同じチェックを回す（--check）",
		"patternSources": [
			"blockedTerms",
			"submoduleNames",
			"localHome"
		],
		"patternSourcesNote": "ゲートに使う値を manifest に持たない。このファイル自身が公開対象なので、ここに固有名や repo 名を書けばそれ自体が漏洩経路になる。実装（harness_lib/public_slim.py の PATTERN_SOURCES）が source ごとに実行時へ解決する: blockedTerms は .env の BLOCKED_TERMS（.env.example が契約を示し、.githooks/pre-commit と値を共有する既存 SSOT）、submoduleNames は .gitmodules に宣言された submodule の URL から導いた名前、localHome は HOME の絶対パス（ユーザー名の裸の文字列で当てると GitHub Actions の runner が hook-runner 等に誤爆する）",
		"failClosed": true,
		"failClosedNote": "blockedTerms が空または読めない場合は build を落とす（exit 2）。.env は追跡外なので、未設定の環境（clone 直後・CI）では検出なしで通ってしまう。公開は不可逆なので、パターンが 0 件のまま生成させない",
		"suspiciousPatterns": [
			"/Users/"
		],
		"suspiciousPatternsNote": "fail させず一覧を出すだけ。実測では例示パス（/Users/harunon/, /Users/example/, /Users/x/）のみ"
	}
}
